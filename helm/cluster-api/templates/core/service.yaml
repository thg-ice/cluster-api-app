apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    giantswarm.io/monitoring: "true"
    giantswarm.io/monitoring-port: "{{ .Values.ports.metrics }}"
  labels:
    {{- include "labels.common.core" . | nindent 4 }}
  name: {{ include "resource.core.name" . }}
  namespace: {{ include "resource.default.namespace" . }}
spec:
  ports:
  - name: metrics
    port: {{ .Values.ports.metrics }}
    targetPort: metrics
  - name: webhook
    port: {{ .Values.ports.webhook }}
    targetPort: webhook
  selector:
    {{- include "labels.selector.core" . | nindent 4 }}
    control-plane: controller-manager
